# Build the provisioning image
CONFIG_XIP=n
in a local domain application

# flash the provisioning image to RAM
nrfutil device x-execute-batch --batch-path ~/ncs/core_fw/secdom/scripts/generated_nrfutil_batch.json --serial-number $(nrfjprog -i)

# Set the PC and start the local domain CPU

nrfutil device go --traits jlink --core Application --program-counter ? --x-family nrf54h --log-level trace

set $pc = 0x2f014754

# ?
python ~/ncs/core_fw/secdom/scripts/simple.py bootmode-set --opcode debugwait
python ~/ncs/core_fw/secdom/scripts/simple.py debug-reset --secdom

stepping through the whole program works

# Do we need to set the SP?
